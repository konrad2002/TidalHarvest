import { GridTileDirective } from './grid-tile.directive';

describe('GridTileDirective', () => {
  it('should create an instance', () => {
    const directive = new GridTileDirective();
    expect(directive).toBeTruthy();
  });
});
