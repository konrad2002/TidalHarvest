import { FieldPopupDirective } from './field-popup.directive';

describe('FieldPopupDirective', () => {
  it('should create an instance', () => {
    const directive = new FieldPopupDirective();
    expect(directive).toBeTruthy();
  });
});
